@model CarSalesSystem.DTOs.DealerDto

@{
    ViewData["Title"] = "Become a Dealer";
}

<section class="section-shell section-shell-tight">
    <div class="container-xl">
        <div class="section-head">
            <div>
                <span class="eyebrow">Dealer Profile</span>
                <h1>Create Your Showroom</h1>
                <p class="section-copy">Set up your public seller profile so buyers can discover your listings and trust your business.</p>
            </div>
        </div>

        <div class="form-shell">
            <div class="form-card form-card-narrow">
                <form method="post" class="stack-form">
                    @Html.AntiForgeryToken()
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                    <div>
                        <label asp-for="Name"></label>
                        <input asp-for="Name" class="form-control" placeholder="Ivan Petrov" />
                        <span asp-validation-for="Name" class="small text-danger"></span>
                    </div>

                    <div>
                        <label asp-for="CompanyName"></label>
                        <input asp-for="CompanyName" class="form-control" placeholder="Auto House Sofia" />
                        <span asp-validation-for="CompanyName" class="small text-danger"></span>
                    </div>

                    <div>
                        <label asp-for="PhoneNumber"></label>
                        <input asp-for="PhoneNumber" class="form-control" placeholder="+359 88 123 4567" />
                        <span asp-validation-for="PhoneNumber" class="small text-danger"></span>
                    </div>

                    <div class="action-row">
                        <a asp-action="GetAll" class="btn btn-outline-secondary">Back to Dealers</a>
                        <button type="submit" class="btn btn-dark">Create Dealer Profile</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
