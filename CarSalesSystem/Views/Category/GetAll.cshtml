@using CarSalesSystem.DTOs
@using CarSalesSystem.Data.Model
@model List<CategoryDto>

<a asp-controller="Category" asp-action="GetAll" class="btn btn-primary mb-3 top-0 left-0" type="submit">GoBack</a>



<div class="row">
	@foreach (var category in Model)
	{
		<div class="col-md-4">
			<div class="card mb-3">
				<div class="card-body text-center">
					<h5 class="card-title text-center"> @category.Name</h5>
					

					<a asp-controller="Category" asp-action="GetAll" asp-route-id="@category.Id" class="btn btn-warning">GetAll</a>
					@* <a asp-controller="Product" asp-action="ProductsByCategory" asp-route-categoryId="@product.Id" class="btn btn-primary">Products by category</a>
				<a asp-controller="Category" asp-action="Delete" asp-route-id="@dealer.Id" class="btn btn-danger" onclick="myFunction() ">Delete</a> *@
				</div>
			</div>
		</div>
	}
</div>
